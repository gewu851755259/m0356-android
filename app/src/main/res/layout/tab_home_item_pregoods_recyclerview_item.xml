<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:background="@color/nc_white"
              android:gravity="center"
              android:orientation="vertical">

    <ImageView
        android:id="@+id/ImageViewImagePic01"
        android:layout_width="@dimen/main_pregoods_size"
        android:layout_height="@dimen/main_pregoods_size"
        android:adjustViewBounds="true"
        android:padding="1dp"
        android:background="@drawable/dic_goods_gridview_pic_bg"
        android:scaleType="fitCenter"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingLeft="3dp"
        android:paddingRight="3dp">

        <TextView
            android:id="@+id/goodsPriceID"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:singleLine="true"
            android:textColor="@color/nc_red"
            android:textSize="16dp"/>

        <TextView
            android:id="@+id/goodsOldPriceID"
            style="@style/NCText"
            android:layout_marginTop="2dp"
            android:singleLine="true"
            android:textSize="12dp"/>
    </LinearLayout>
</LinearLayout>